<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Tambah Data
  body
    include partials/navbar.pug
    #container
      #wrap
        form(action="/tambah-data", method="post")
          input(type="text", name="nim" placeholder='NIM' class="input nim")
          input(type="text", name="nama" placeholder="Nama" class="input nama")
          input(type="email", name="email" placeholder="Email" class="input email")
          input(type="text", name="fakultas" placeholder="Fakultas" class="input fakultas")
          button(type="submit" class="input submit") Submit Data